/*
 * firebase.js — your project's Firebase configuration.
 *
 * These values are NOT secret. Firebase web config always ships in public
 * client code; access is controlled by the Firestore security rules
 * (see firestore.rules), not by hiding these values. Safe to commit.
 *
 * To point this app at a different Firebase project (e.g. during handoff),
 * replace the object below with the config from:
 *   Firebase console -> Project settings -> Your apps -> Web app -> SDK config.
 * See docs/HANDOFF.md.
 */
window.FIREBASE_CONFIG = {
  apiKey: 'AIzaSyDgbbyiefs_i-3XqRTuiFaEhZ00XX2VYeI',
  authDomain: 'rush-elo-app.firebaseapp.com',
  projectId: 'rush-elo-app',
  storageBucket: 'rush-elo-app.firebasestorage.app',
  messagingSenderId: '1037405158920',
  appId: '1:1037405158920:web:1206f3603f01331835dbbd',
  measurementId: 'G-DTRDM8Q098'
};
